import HelloWorld from '@/components/HelloWorld.vue';

// mount at #hello-world.
new HelloWorld();

// or get the element and append it to where you want.
// document.querySelector('#app')!.append(new HelloWorld().$el);
